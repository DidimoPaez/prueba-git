(ns prueba-git.core)

(defn foo
  "I don't do a whole lot. hola"
  [x]
  (println x "Hello, World!"))
(defn fibonacci [n]
  (if (< n 2)
    1
    (* (fibonacci (n - 1)) (fibonacci (n - 1)))))

(defn foo
      "I don't know what to do"
      [x]
      (println x "Hello, MUNDO!"))

(defn suma
  "I don't do a whole lot. hola"
  [x]
  (println x "Hello, World!"))

